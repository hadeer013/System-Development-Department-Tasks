@* @model List<Product>
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
<a asp-action="Create" class="btn btn-outline-primary">Add New Product</a>

@if(Model.Count>0)
{
    @foreach (var product in Model)
    {
        <div class="form-control">
            <div class="card" style="width: 15rem;">
                <img class="card-img-top" src="/images/@product.ImageUrl" alt="Card image cap">
                <div class="card-body">
                    <h5 class="card-title">Product</h5>
                    <p class="card-text">Some content about product details........</p>
                    <a asp-action="Details" asp-route-Id="@product.Id" class="btn btn-primary">Details</a>
                    <a asp-action="Edit" asp-route-Id="@product.Id" class="btn btn-warning">Edit</a>
                    <a asp-action="Delete" asp-route-Id="@product.Id" class="btn btn-danger">Delete</a>
                </div>
            </div>

        </div>
    }
}
else
{
    <h3>No products in Repo</h3>
} *@

@*                     @model List<Product>
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
<a asp-action="Create" class="btn btn-outline-primary">Add New Product</a>

@if (Model.Count > 0)
{
    @for (int i = 0; i < Model.Count; i += 3)
    {
        <div class="row">
            @for (int j = i; j < Math.Min(i + 3, Model.Count); j++)
            {
                <div class="col-md-4">
                    <div class="form-control">
                        <div class="card" style="width: 15rem;">
                            <img class="card-img-top" src="/images/@Model[j].ImageUrl" width="50px" height="50px" alt="Card image cap">
                            <div class="card-body">
                                <h5 class="card-title">@Model[j].Name</h5>
                                <p class="card-text">@Model[j].Description</p>
                                <a asp-action="Details" asp-route-Id="@Model[j].Id" class="btn btn-primary">Details</a>
                                <a asp-action="Edit" asp-route-Id="@Model[j].Id" class="btn btn-warning">Edit</a>
                                <a asp-action="Delete" asp-route-Id="@Model[j].Id" class="btn btn-danger">Delete</a>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
}
else
{
    <h3>No products in Repo</h3>
}
 *@

                                @model List<Product>
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
<a asp-action="Create" class="btn btn-outline-primary">Add New Product</a>

@if (Model.Count > 0)
{
    @for (int i = 0; i < Model.Count; i += 4)
    {
        <div class="row">
            @for (int j = i; j < Math.Min(i + 4, Model.Count); j++)
            {
                <div class="col-md-3">
                    <div class="form-control">
                        <div class="card" style="width: 15rem;">
                            <img class="card-img-top" src="/images/@Model[j].ImageUrl" alt="Card image cap" style="width: 100%; height: 200px; object-fit: cover;">
                            <div class="card-body">
                                <h5 class="card-title">@Model[j].Name</h5>
                                <p class="card-text">@Model[j].Description</p>
                                <a asp-action="Details" asp-route-Id="@Model[j].Id" class="btn btn-primary">Details</a>
                                <a asp-action="Edit" asp-route-Id="@Model[j].Id" class="btn btn-warning">Edit</a>
                                <a asp-action="Delete" asp-route-Id="@Model[j].Id" class="btn btn-danger">Delete</a>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
}
else
{
    <h3>No products in Repo</h3>
}
